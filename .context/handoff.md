# Handoff

## Current state — 2026-09-09

- Stockholm is deployed as the primary trial; Cape Town remains a live slower backup. Frankfurt is retired; its catalog entry is only a recipe. Profile `personal`, account `757999402784`, stack name `GhostlinePoc` scoped by region. [Stockholm launch](../docs/launch-stockholm.md), [Cape Town launch](../docs/launch-cape-town.md).
- Stockholm `eu-north-1`: host `i-033493f9d064f8b00`, ENI `eni-037352399599d6cc9`, encrypted 20 GiB gp3 `vol-00624392e421f4f1a`, Ubuntu AMI `ami-035c8a091035e710a`. Xray `16.170.38.152` / `eipalloc-0849e55ead905822b` / private `10.77.0.69`; AWG/admin `13.50.178.121` / `eipalloc-0c6fc191dc53dd019` / private `10.77.0.13`.
- Cape Town `af-south-1` is unchanged: host `i-017247cce7d2bf84f`, Xray `16.28.130.178`, AWG/admin `15.240.94.162`. Earlier owner macOS/iOS protocol tests passed; Anthony now reports both protocols too slow. Preserve this backup and its credentials/profiles.
- Each region has one t3.small/Ubuntu 24.04 host, two EIPs and manual switching. No HA, ECR/ECS, Parameter Store backend, host IAM, fleet controller or automatic failover. Cost tags remain Project=ghostline, Environment=prod, shared compute/disk/network and protocol-owned EIPs.

## Latest client interruption / next work

1. Mac Stockholm Xray passed real exit/HTTPS. Anthony authorized guarded AWG retesting after the earlier interruption. Two native AWG reconnects now passed: initial 9/9 pings and three DNS/HTTPS/exit rounds, then about 128 seconds with 30/30 HTTPS and expected-exit checks, 89/90 pings and 29/30 direct DNS queries. One ping/DNS loss recovered next round; the original outage was not reproduced. Server metadata confirmed a new handshake ~100 seconds into the longer trial and later probes passed. No server/profile fix was applied; earlier cause remains unknown.
2. Verified Amnezia's local newline-JSON daemon API (`status`, `deactivate`) and armed an independent timeout before each test, with early failed-connectivity recovery and unconditional cleanup. Both trials automatically disconnected and restored direct exit `5.195.76.221`; UI shows Connect. No timer remains armed, persistent logging was not enabled, and no security settings changed. Task-local diagnostics/script are in `.local/diagnostics/stockholm-awg/`; consult the knowledge note before reuse.
3. Native profiles are `Ghostline Stockholm REALITY` and `Ghostline Stockholm AWG`; Cape Town profiles remain. iOS QR/link files are prepared. Native Mac AWG agent checks now pass, including a 5 MiB HTTPS transfer in 1.73 seconds; owner practical use/performance and iOS remain pending. No unreported DNS-leak, IPv6, concurrency, video or reboot claims.
4. Anthony intermediates LastPass. Save `.local/keys/ghostline-poc-stockholm`, `.local/recovery/stockholm-runtime.json`, its `.clients/{macos,ios}.json`, and `stockholm-awg/{awg0,macos,ios}.conf`. Cape Town material remains unchanged. Vault saves unconfirmed.

## Implemented and verified

- Reuse `EndpointStack` per explicit target. `park <target>` leaves only CloudFormation-owned retained EIPs (plus free CDK metadata); `deploy` rebuilds host/networking and reattaches them. `destroy` captures live allocation outputs cross-checked against EIP resource addresses, deletes only that stack ARN, waits, verifies ownership/unattached state, releases those IPs, and supports partial retry via local pending-release record. Prefer full release for abandoned PoC exits.
- Live Stockholm initial deploy → park → deploy passed. Both allocation IDs/public IPs survived; host/ENI/private IPs changed. Regional disk inventory contains only the rebuilt disk; parked EIPs preserved project/protocol/CloudFormation tags. This test occurred before runtime installation. Full-release safeguards are tested locally; Stockholm allocations were not actually released.
- Explicit fresh Xray generation uses inspected Amnezia 5.0.1.5 REALITY/Vision settings and independent regional/per-device identity. Standard VLESS links and local QR files accompany native JSON. AWG generation is unchanged. Installation never regenerates credentials. Client links omit trailing VLESS newlines because Amnezia includes them in the label fragment.
- Both Stockholm server runtimes passed configuration equality, binding, kernel SNAT and actual EIP egress checks. Pinned images built using cache; disposable Xray client validation/start/restart/malformed-exit and real AWG peer-handshake tests passed.
- Commit-prep reran the Node 24 gate: typecheck, all three offline targets and 82 tests passed. Git-visible credential scan, 103 local Markdown links, dependency/lockfile consistency and diff whitespace checks passed. The last Stockholm live CDK diff was clean; no cloud or client changes were made during commit prep. Cape Town live diff has no resource changes; only the informational SSH output would change to its existing admin/AWG IP. No Cape Town deployment was run.
- Follow `AGENTS.local.md`: local Node 24.20.0, cache `/tmp/ghostline-npm-cache`, omit only unsupported npm proxy duplicates. AWS/SSH/Docker require sandbox escalation. Keep Mac VPN disconnected during admin; never widen operator `5.195.76.221/32`.
- Explicit `runtime <target> trust` validates current AWS instance/ENI/EIP and pins its authenticated console keys, archiving prior trust if changed. Ordinary SSH never accepts arbitrary new keys.
- This work began with six dirty region-research files at `23a14a8`; preserve that research in the proposed commit alongside Stockholm/lifecycle/runtime changes. Index untouched, no commit made. Agent instructions now scope one host per region and name the lifecycle commands; decision breadcrumb recorded.
